import { type Metadata } from 'next';

export const metadata: Metadata = {
  title: 'About App Assistant Chef',
  description: 'Information about App Assistant Chef',
};

export default function Contacts() {
  return <h2>Contacts</h2>;
}
